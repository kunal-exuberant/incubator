
package likedriving.TechStacks.ApacheStorm;
/*
import org.apache.storm.Config;
import org.apache.storm.LocalCluster;
import org.apache.storm.topology.TopologyBuilder;

import java.util.HashMap;
import java.util.Map;

public class SimpleTopology {

    static TopologyBuilder topologyBuilder;

    public static void main(String[] args) {

        topologyBuilder = new TopologyBuilder();
        topologyBuilder.setSpout("1", new SimpleSpout());
        topologyBuilder.setBolt("2", new SimpleBolt());

        Map conf = new HashMap();

        conf.put(Config.STORM_ID, "4");

        LocalCluster localCluster = new LocalCluster();
        localCluster.submitTopology("3", conf, topologyBuilder.createTopology());
    }

}


*/
